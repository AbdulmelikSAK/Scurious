<%= simple_form_for (@project) do |f| %>
        <%= f.input :title %>
        <%= f.input :description %>
        <%= f.input :evolution %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
